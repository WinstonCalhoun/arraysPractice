var students = ["Brad", "Ryan", "Josh", "Eric"];
var testScores = [35, 60, 75, 90];
var pass = [true, false]
var retake = [true, false]

students.pop()
// extracts the item at the end

testScores.shift()
// extracts and item from the begining

students.push(..."John")
// adds and item at the end

[Each element in the array has an index position stating with 0]
[In the students array, Brad would be index position 0 and Eric
would be indexposition 3]
